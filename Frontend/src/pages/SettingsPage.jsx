import { Database, EyeOff, Server, ShieldCheck } from 'lucide-react'
import { Card, CardHeader, PageHeader } from '../components/ui'

export default function SettingsPage() { return <><PageHeader eyebrow="Configuration" title="Settings" description="Runtime thresholds and secrets are controlled with server environment variables."/><div className="settings-grid"><Card><CardHeader title="Privacy defaults"/><div className="setting-row"><ShieldCheck/><div><strong>Metadata-only retention</strong><p>Continuous raw video is never stored.</p></div></div><div className="setting-row"><EyeOff/><div><strong>Protected biometrics</strong><p>Embeddings stay behind the internal service boundary.</p></div></div></Card><Card><CardHeader title="Service configuration"/><div className="setting-row"><Database/><div><strong>PostgreSQL + Redis</strong><p>History is durable; live state is transient.</p></div></div><div className="setting-row"><Server/><div><strong>Environment managed</strong><p>See .env.example files for all supported settings.</p></div></div></Card></div></> }
